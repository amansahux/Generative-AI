/**
 * @file ai/chains/chat.chain.js
 * @description Placeholder for the main conversational AI chain.
 * Wire up the LLM, memory, and prompt template here to form an LCEL chain.
 */

/**
 * Build and return the chat chain.
 * No AI implementation — placeholder only.
 *
 * @returns {null}
 */
export const buildChatChain = () => {
  // TODO: Compose chain using model | prompt | memory | parser
  return null;
};
