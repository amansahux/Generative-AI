/**
 * @file config/ai.js
 * @description Placeholder AI configuration.
 * AI provider credentials and model settings will be defined here.
 * No AI implementation is present in this boilerplate.
 */

/**
 * AI configuration placeholder.
 * Populate with provider-specific settings (e.g. model name, API key reference, temperature).
 *
 * @type {Object}
 */
const aiConfig = {
  // model: process.env.AI_MODEL,
  // apiKey: process.env.AI_API_KEY,
  // temperature: 0.7,
};

export default aiConfig;
