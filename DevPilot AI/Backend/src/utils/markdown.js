/**
 * @file utils/markdown.js
 * @description Placeholder utility for Markdown processing.
 * Render, sanitize, or transform Markdown strings here.
 */

/**
 * Convert a Markdown string to plain text.
 *
 * @param {string} markdownText - The raw Markdown content.
 * @returns {string} Plain text (placeholder — returns input unchanged).
 */
export const markdownToPlainText = (markdownText) => {
  // TODO: Implement Markdown → plain-text conversion
  return markdownText;
};

/**
 * Sanitize a Markdown string to remove potentially dangerous HTML.
 *
 * @param {string} markdownText - The raw Markdown content.
 * @returns {string} Sanitized Markdown (placeholder — returns input unchanged).
 */
export const sanitizeMarkdown = (markdownText) => {
  // TODO: Implement Markdown sanitization
  return markdownText;
};
